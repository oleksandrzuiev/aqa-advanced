const originalNumbers = [1, 2, 3, 4, 5]

const multipliedByIndexNumbers = originalNumbers.map((el, index) => el * index)

console.log("Original numbers", originalNumbers)
console.log("Multiplied by index numbers", multipliedByIndexNumbers)
